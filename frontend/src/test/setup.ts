// Vitest setup — runs before every test file. We import
// jest-dom's matchers so `expect(el).toBeInTheDocument()` is
// available without a per-file import.
import '@testing-library/jest-dom/vitest';
