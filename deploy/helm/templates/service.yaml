apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-toolkit.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    {{- include "devops-toolkit.selectorLabels" . | nindent 4 }}
